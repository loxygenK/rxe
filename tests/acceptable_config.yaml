cmd:
  test:
    args:
      type:
        choice: [core, frontend, types]
        short: t
        multi: true
      snapshot:
        flag:

    run:
      echo "Executing the test for {type} {snapshot|true=(with snapshot)}"
